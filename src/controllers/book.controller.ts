import { IncomingMessage, ServerResponse } from 'http';

export const getBooks = async (req: IncomingMessage, res: ServerResponse) => {};

export const getBook = async (req: IncomingMessage, res: ServerResponse) => {};

export const createBook = async (req: IncomingMessage, res: ServerResponse) => {};

export const updateBook = async (req: IncomingMessage, res: ServerResponse) => {};

export const deleteBook = async (req: IncomingMessage, res: ServerResponse) => {};