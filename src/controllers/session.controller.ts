import { IncomingMessage, ServerResponse } from 'http';

export const getSessions = async (req: IncomingMessage, res: ServerResponse) => {};

export const getSession = async (req: IncomingMessage, res: ServerResponse) => {};

export const createSession = async (req: IncomingMessage, res: ServerResponse) => {};

export const updateSession = async (req: IncomingMessage, res: ServerResponse) => {};

export const deleteSession = async (req: IncomingMessage, res: ServerResponse) => {};